const words = ['Environmental', 'Systems', 'Research', 'Institute'];

const maslarga = words.reduce((acumulador, cadena) => {
  if (acc.length > current.length) {
    return acumulador
  } else {
    return cadena
  }
});

console.log(`La palabra más larga es ${maslarga}`);